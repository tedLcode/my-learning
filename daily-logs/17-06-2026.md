# 📅 2026-06-17

## 🏢 What I Observed at Work Today
- Continued Lambda quarantine routing logic refinement
- Hit a pipeline blocker: the Groovy deployment script was working correctly on the feature branch and Lambda build branch, but failing on the develop branch
- Paired with Somas to diagnose the issue — root cause was that the Groovy script was being picked up from a different repository than expected
- Resolved the blocker by adding explicit properties to the pipeline configuration (e.g., `COV = lambda`) to ensure the correct repo and build parameters were referenced during the develop branch deployment

## 📚 What I Studied Today
- Topic: Groovy Pipeline Configuration and Multi-Repo Builds
  - What I understood: When a Jenkins pipeline pulls Groovy from a shared library or a different repository, branch-level behavior can diverge — the feature branch may resolve the script correctly while develop picks up a stale or different version
  - What I understood: Explicitly setting properties in the pipeline definition (rather than relying on implicit resolution) removes ambiguity about which repo and which version of the Groovy script is being used
  - What I understood: This kind of blocker is common in multi-repo CI/CD setups — the fix is usually about making the resolution path explicit, not about changing the logic itself

- Topic: Branch Strategy and Deployment Pipeline Alignment
  - What I understood: Feature branches and Lambda build branches often have isolated pipeline configurations that work cleanly; the develop branch is where integration issues surface because it aggregates multiple streams
  - What I understood: Debugging pipeline issues requires checking not just the code but also what the pipeline is actually resolving at runtime — repo source, branch ref, and property values all matter

## 💻 What I Built / Practiced
- Diagnosed and resolved a Groovy pipeline blocker on the develop branch alongside Somas
- Added explicit pipeline properties to lock in correct repo resolution for the Lambda deployment
- Confirmed quarantine routing Lambda deployment proceeds correctly on develop branch after the fix

## ❓ One Question I Want to Ask Tomorrow
- Are there other places in the pipeline configuration where implicit repo resolution could cause similar issues across different branches?

## 🔗 Connections I Made
- Explicit property declarations in a Groovy pipeline are the same discipline as explicit imports in Python — relying on implicit resolution works until it doesn't, and the failure is always confusing when it happens
- The develop branch integration problem mirrors what happens in database deployments when environment-specific scripts are missing — the isolated path works, the integrated path breaks

## ⭐ One Thing I'm Proud of Today
- Paired effectively with Somas to get to root cause quickly — identified that the issue was repo resolution, not logic, and fixed it with a targeted property addition rather than a broader rewrite
